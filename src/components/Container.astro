---
import clsx from 'clsx'

interface Props { className?: string}
const {className, ...props}= Astro.props
---
<div class={clsx('mx-auto max-w-7xl px-4 sm:px-6 lg:px-8', className)} {...props}><slot /></div>
